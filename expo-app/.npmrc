# Ignore versions published in the last 3 days. A compromised release is
# usually caught and unpublished within hours, so waiting keeps the worst of
# the npm supply-chain attacks out of installs and lockfile updates.
min-release-age=3
